Django>=5.0,<5.2
djangorestframework>=3.15
django-environ>=0.11
django-cors-headers>=4.4
argon2-cffi>=23.1
psycopg[binary]>=3.2
boto3>=1.34
django-storages>=1.14
beautifulsoup4>=4.12
lxml>=5.0
# GJU credential verifier drives a real browser (MyGJU's WAF blocks plain
# headless). After install, run once: `playwright install chromium`.
playwright>=1.44
